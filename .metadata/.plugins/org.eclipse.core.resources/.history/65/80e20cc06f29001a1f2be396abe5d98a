package ie.salesforce.extract.service;

public interface CustRepService {

	/**
	 * Start the job that creates the customer and representative list.
	 * @return true if the job was started successfully
	 */
	String runSalesRepresentativesJob();

}
